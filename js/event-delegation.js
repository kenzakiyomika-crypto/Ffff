'use strict';
/**
 * event-delegation.js — auto-generated by v14 fix pass
 * Replaces inline onclick handlers with a single delegated listener.
 * Each data-action value is eval'd in the context of the click event.
 */

document.addEventListener('click', function(event) {
  const el = event.target.closest('[data-action]');
  if (!el) return;
  const action = el.getAttribute('data-action');
  if (!action) return;
  try {
    // eslint-disable-next-line no-new-func
    (new Function('event', action))(event);
  } catch (err) {
    console.warn('[EventDelegation] Action failed:', action, err);
  }
});
